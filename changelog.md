## Changelog

### 1.2.6
- Atualizado `config.json` para incluir `configUrl`, `downloadUrl`, `changelogUrl` e `versionHistoryUrl`.
- Mantido `shortName` como `meshdrive`.
- Atualizado `meshCentralCompat` para `>=1.2.1`.
- Atualizado log interno de carregamento para 1.2.6.

# Changelog

## 1.2.4
- Configuração centralizada em `shares.json` com todos os domínios no mesmo arquivo.
