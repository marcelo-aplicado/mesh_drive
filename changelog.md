# Changelog

## 1.2.6
- Corrigida a rota da API do editor de contatos.
- `/meshcontacts/api/books` agora responde JSON corretamente quando Express remove o prefixo da rota montada.
- A tela `/meshcontacts` lista os Address Books e os arquivos `.vcf` do tenant atual.
